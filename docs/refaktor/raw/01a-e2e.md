# RAW 01a — raport agenta f1-e2e (przepływ krytyczny E2E)

> Surowy raport subagenta read-only (Faza 1). Zweryfikowany na HEAD `fcf1342`. Synteza → `../01-stan-obecny.md`.

# FAZA 1 — PRZEPŁYW KRYTYCZNY E2E (ingest → przypisanie → ETA)

**Baza:** HEAD `fcf1342` (master), repo `/root/.openclaw/workspace/scripts/dispatch_v2`. Read-only. Wszystkie `plik:linia` zweryfikowane grepem na HEAD. Zgodność z mapą 10 warstw (`docs/ARCHITECTURE.md §2`) i enrichment (`eod_drafts/2026-06-27/…ENRICHMENT.md`) — potwierdzam, uzupełniam liniami i prostuję dryf.

Konwencja: **FAKT** = zweryfikowany `plik:linia`. **HIPOTEZA:** = niezweryfikowane + jak sprawdzić. Ścieżki względne od korzenia repo, o ile nie zaznaczono inaczej.

---

## (a) TABELA KROKÓW

| # | Krok | Proces / serwis systemd | Funkcja (symbol) | plik:linia (HEAD) | Wejście → wyjście |
|---|---|---|---|---|---|
| 1 | Poll gastro + diff | `dispatch-panel-watcher` (`-m dispatch_v2.panel_watcher`) | `tick` → `_diff_and_emit` | `panel_watcher.py:2441` → `:1106` | HTML panelu → wykryte zmiany ID |
| 1a | Emit NEW_ORDER (idempotent) | panel-watcher | `emit("NEW_ORDER",…)` | `panel_watcher.py:1359` (event_id `:1358`) | nowy zid → event w `events.db` |
| 1b | Zapis stanu zlecenia | panel-watcher | `update_from_event` | `panel_watcher.py:1368` → `state_machine.py:612` | event → `orders_state.json` (fcntl `_guarded_write` `:272`, `_read_state_strict` `:372`) |
| 2 | Konsumpcja NEW_ORDER (pętla) | `dispatch-shadow` (`-m dispatch_v2.shadow_dispatcher`) | `_tick` | `shadow_dispatcher.py:1107` | `event_bus.get_pending(["NEW_ORDER"])` `:1110` |
| 2a | Snapshot floty (telemetria) | shadow (dawniej też panel-watcher) | `dispatchable_fleet` | `shadow_dispatcher.py:1114` → `courier_resolver.py:1511` (baza `build_fleet_snapshot` `:787`) | GPS+grafik+fallback → `{cid:CourierState}` |
| 3 | Ocena zlecenia (10 warstw) | shadow | `assess_order` → `_assess_order_impl` | `shadow_dispatcher.py:1083` → `dispatch_pipeline.py:3565` (impl ~`:3641`) | order_event+fleet → `PipelineResult` |
| 3a | **Geokod HARD** (defense gate) | shadow | pickup_coords None/(0,0) → SKIP | `dispatch_pipeline.py:3728-3746` (`reason="no_pickup_geocode"`) | brak coords → `verdict=SKIP` |
| 3b | **Early-bird HARD** (≥próg min) | shadow | próg `_early_bird_threshold_min` | `dispatch_pipeline.py:3783-3827` (próg `:2750`) | ≥60 min naprzód → `verdict=KOORD` |
| 3c | Feasibility HARD (per kurier) | shadow (w wątkach) | `check_feasibility_v2` | wywołanie `dispatch_pipeline.py:4108` → `feasibility_v2.py:430` | kandydat → `MAYBE`/`NO` (R6 35/40, SLA, shift) |
| 3d | Scoring SOFT (~19 kar) | shadow (w wątkach) | `scoring.score_candidate` | wywołanie `dispatch_pipeline.py:4303` → `scoring.py:189` | kandydat MAYBE → `final_score` |
| 3e | Pętla równoległa | shadow | `_v327_eval_courier` (closure) w `ThreadPoolExecutor` | def `:3869`/inner `:3884`; map `:6030`/`:6060` | 10 kurierów → lista `Candidate` |
| 3f | Selekcja SOFT | shadow | filtr `MAYBE`+sort, potem re-order pipeline | `dispatch_pipeline.py:6287-6388` | Candidates → uporządkowana `feasible` |
| 3g | **Werdykt KOORD/PROPOSE** | shadow | bramki gate-first-match | `dispatch_pipeline.py:6799-7405` | `feasible` → `PipelineResult.verdict` |
| 4 | Serializacja decyzji | shadow | `_serialize_result` (LOC B) + `_serialize_candidate` (LOC A) | `shadow_dispatcher.py:478` / `:250`; propag. `:235` | result → dict rekordu |
| 4a | Zapis kanonicznego logu | shadow | `_append_decision` (single-writer) | `shadow_dispatcher.py:1348` (def `:864`) → `scripts/logs/shadow_decisions.jsonl` (path z cfg `:1540`) | rekord → append jsonl |
| 4b | Zapis propozycji (Opcja B) | shadow | `pending_proposals_store.upsert_proposals` | `shadow_dispatcher.py:1378-1387` → `pending_proposals_store.py:190` | PROPOSE → `dispatch_state/pending_proposals.json` |
| 5 | **Akceptacja 1-klik** (konsola) | `nadajesz-panel` (uvicorn :8000, repo `nadajesz_clone/panel`) | `assign_courier` → subprocess `gastro_assign.py` | `…/integrations/ziomek/assign.py:63` (`_build_cmd` `:35`) → `scripts/gastro_assign.py:215` | operator klika → login+POST gastro |
| 5a | Zapis do panelu gastro | gastro_assign (subprocess venv Ziomka) | POST `/przypisz-zamowienie` + `--time`/`--keep-time` | `gastro_assign.py:122`, `:226`/`:228`, czas `:289` | kurier+czas_kuriera → panel gastro |
| 5b | Wykrycie przypisania + zapis planu | panel-watcher | `_save_plan_on_assign_signal` | `panel_watcher.py:641` (woł. `:1427,:1540,:1572,:1684,:2397`) | COURIER_ASSIGNED → plan z pending |
| 5c | Recanon po przypisaniu | panel-watcher (env panel-watchera) | `recanon_courier`+`redecide_courier` | `panel_watcher.py:654`/`:663` → `plan_recheck.py:2137`/`:2074` | surowa sekwencja → kanon |
| 6 | Re-sekwencja planów (cykl) | `dispatch-plan-recheck` (timer 5 min) | `run_recheck` | `plan_recheck.py:2451`; kanon `_apply_canon_order_invariants` `:1739` | plany → re-canon + retime |
| 6a | Odświeżenie ETA cache | plan-recheck (env plan-rechecka) | `_refresh_live_eta_from_plans` (za flagą) | `plan_recheck.py:2317` (call `:2544`) | plany → `live_order_eta.json` |
| 6b | Frozen pickup ETA (apka) | `courier-api` (:8767, `main.py`) | `_attach_fallback_eta` / `_committed_pickup_eta` | `courier_api/courier_orders.py:846` / `:665`; flaga `config.py:113` (`FROZEN_PICKUP_ETA` default „1") | plan → kroki z ETA committed |
| 6c | Kolejność trasy apki | courier-api | `route_podjazdy.order_podjazdy` | `courier_orders.py:1142`; flaga `config.py:68` (`APP_ROUTE_FROM_CONSOLE` default „0") | plan → podjazdy |
| 6d | Render trasy konsoli | nadajesz-panel | `fleet_state._build_route` / `_order_from_plan_seq` | `…/ziomek/fleet_state.py:395` / `:342` | plan → widok koordynatora |
| 7 | Zdarzenia pickup/deliver/return | panel-watcher (4 handlery recanon) | patrz sekcja (c)-recanon | `panel_watcher.py:668/703/731` | zmiana worka → prune+recanon |

---

## (b) SEKWENCJA HAPPY-PATH (numerowana)

1. **Restauracja składa zlecenie w gastro.** `dispatch-panel-watcher` odpytuje `gastro.nadajesz.pl` w pętli (`panel_watcher.run` `:2574` → `tick` `:2441`), parsuje HTML i porównuje z `orders_state` w `_diff_and_emit` (`:1106`).

2. **Ingest → stan + event.** Dla nowego ID panel-watcher (a) geokoduje adres dostawy (`:1325`, fail→warn), (b) emituje deterministyczny `NEW_ORDER` (`emit(...)` `:1359`, event_id `{zid}_NEW_ORDER_first` `:1358`), (c) woła `state_machine.update_from_event` (`:1368` → `state_machine.py:612`), która robi zapis pod fcntl (`_guarded_write` `:272`). Źródło prawdy o zleceniu = `orders_state.json` (`/root/.openclaw/workspace/dispatch_state/`, POZA repo).

3. **Silnik budzi się na NEW_ORDER.** `dispatch-shadow._tick` (`shadow_dispatcher.py:1107`) pobiera `event_bus.get_pending(event_types=["NEW_ORDER"])` (`:1110`) i buduje snapshot floty `dispatchable_fleet()` (`:1114` → `courier_resolver.py:1511`; wzbogaca `build_fleet_snapshot` `:787` o grafik/GPS/last-known-pos).

4. **assess_order — 10 warstw** (`dispatch_pipeline.py:3565`/impl `:3641`), kolejność ZAKODOWANA:
   - **HARD geokod:** brak `pickup_coords` (None/(0,0)) → `verdict=SKIP reason=no_pickup_geocode` (`:3728-3746`).
   - propagacja `czas_kuriera_warsaw` jako kotwicy odbioru (`:3755-3768`).
   - **HARD early-bird:** `minutes_ahead ≥ _early_bird_threshold_min()` (`:3786`, próg `:2750`) → `verdict=KOORD` (`:3818`), wcześniej opcjonalny forward-shadow (`:3791`).
   - telemetria/kontekst floty: `get_pickup_ready_at` (`:3829`), `_build_fleet_context_from_snapshot` (`:3852`).
   - **pętla równoległa 10 kurierów** w `ThreadPoolExecutor` (`:6030`/`:6060`), każdy przez `_v327_eval_courier` (`:3869`)→`_inner` (`:3884`):
     - **HARD feasibility:** `check_feasibility_v2` (`:4108` → `feasibility_v2.py:430`) — R6 35/40 tier-aware, SLA, shift; sukces = `MAYBE`.
     - **SOFT scoring:** `scoring.score_candidate` (`:4303` → `scoring.py:189`) — suma ~19 kar/bonusów → `final_score`; `feasibility_verdict` zapisywany w konstruktorze `Candidate` (`:6022`).

5. **Selekcja** (`dispatch_pipeline.py:6287+`): filtr `MAYBE` + sort po `-score` (`:6287-6288`), potem in-place re-order: v325 nowy-kurier (`:6292`) → v326 speed/load/multistop (`:6295/:6298/:6307`) → a2 (`:6302`) → gps_age (`:6304`) → `_demote_blind_empty` MUSI być ostatni demote (`:6316`) → **strażnik HARD `_assert_feasibility_first`** (`:6320`) → tiering late-pickup (`:6335-6369`) → reorder OBJM_LEXR6 (`:6377-6388`, flaga) → E2 PLN A/B (`:6613`, 20% LIVE) → shadow feas-carry (`:6638`) → **LIVE mutacja `FEAS_CARRY_READMIT`** (`:6648-6670`, flaga).

6. **Werdykt** (gate first-match-wins, `:6799-7405`): kolejno `state_likely_stale`→KOORD (`:6799`), `geometry_blind_fallback`→KOORD (`:6839`), `MIN_PROPOSE` low-score→KOORD (`:6877`), `commit_divergence`→KOORD (`:6950`, redirect `:6968`), `difficult_case`→KOORD (`:7005`). Happy-path: **`verdict=PROPOSE`** ze zwycięzcą feasible (`:7073`).

7. **Serializacja + zapis.** `_serialize_result` (`:478`, LOCATION B inline best) + `_serialize_candidate` (`:250`, LOCATION A alternatives), wspólny `_propagate_prefixed_metrics` (`:235`). `_append_decision` (`:1348`, def `:864`, single-writer) dopisuje do `scripts/logs/shadow_decisions.jsonl`. PROPOSE-y zebrane i zapisane atomowo do `pending_proposals.json` (`:1378-1387`).

8. **Powierzchnia = konsola** (`gps.nadajesz.pl/admin`, Telegram uśpiony 26.06). Koordynator widzi propozycję, klika 1-klik.

9. **Akceptacja.** Konsola `assign_courier` (`…/ziomek/assign.py:63`) buduje komendę (`_build_cmd` `:35`) i odpala subprocess `scripts/gastro_assign.py` (`main` `:215`) w venv Ziomka: login + POST `/admin2017/new/orders/przypisz-zamowienie` (`:122`), przekazuje kuriera i **czas** (`--time`/`--keep-time` `:226`/`:228`, `czas_odbioru` `:289`). Audyt każdej próby → `coordinator_assign_audit.jsonl` (`:20`).

10. **Domknięcie pętli.** Panel gastro pokazuje kuriera → następny `tick` panel-watchera wykrywa COURIER_ASSIGNED w `_diff_and_emit` → `_save_plan_on_assign_signal` (`:641`): `_save_plan_on_assign`+`_invalidate_plan_on_bag_change` (`:647-648`) + `recanon_courier(reason="assign")` (`:654`) + `redecide_courier` (`:663`). Plan w `courier_plans.json` egzekwuje kanon.

11. **Aktualizacja ETA.** (a) Cyklicznie `dispatch-plan-recheck` co 5 min (`run_recheck` `:2451`) re-canonuje (`_apply_canon_order_invariants` `:1739`: niesione-front + odbiory wg committed + relax) i za flagą odświeża `live_order_eta.json` (`_refresh_live_eta_from_plans` `:2317/:2544`). (b) Apka (`courier-api :8767`) czyta plan, zamraża odbiór committed (`_attach_fallback_eta` `:846` / `_committed_pickup_eta` `:665`, flaga `FROZEN_PICKUP_ETA` „1" `config.py:113`) i renderuje podjazdy (`route_podjazdy` `:1142`). (c) Konsola renderuje `fleet_state._build_route` (`:395`).

---

## (c) ODGAŁĘZIENIA

**Czasówka (prep ≥60 / early-bird hold).** Osobny serwis `dispatch-czasowka.timer` (1 min) → `czasowka_scheduler.main` (`:596`), buduje flotę przez `dispatchable_fleet()` (`:340`, fix #471036) i woła ten sam `assess_order` (`:341`). Zlecenia early-bird trzymane jako KOORD w silniku (`dispatch_pipeline.py:3818`) aż czasówka w oknie T-60/50/40 je przepuści. Telegram czasówki wyciszony: `CZASOWKA_TELEGRAM_DRYRUN` default „1" (`czasowka_scheduler.py:52`).

**best_effort (pula feasible = 0).** Gdy żaden kandydat nie jest `MAYBE`, ścieżka best_effort (`dispatch_pipeline.py:7136+`): sort `_best_effort_sort_key` (`:610`) po R6-violations; bramki `best_effort_r6_redirect`→KOORD (`:7250`, redirect `:7268`), `be_r6`→KOORD (`:7286`), `be_low`→KOORD (`:7313`); inaczej **best_effort PROPOSE** (SLA-kompromis) (`:7331`). Fallback solo: dodatkowe `check_feasibility_v2` (`:7354`) → PROPOSE (`:7387`). Ostateczny brak → KOORD (`:7403`).

**KOORD (werdykt-hold do koordynatora).** 6 miejsc: early-bird (`:3818`), state_stale (`:6799`), geometry_blind (`:6839`), MIN_PROPOSE low-score (`:6877`), commit_divergence >10 min (`:6950`), difficult_case (`:7005`). KOORD NIE idzie do Telegrama (uśpiony) — jedyna persystencja to `shadow_decisions.jsonl`.

**Paczki (parcel lane, most).** `dispatch-parcel-merge.timer` (30 s, ⚠ NIE `dispatch-parcel-lane-merge` — taka nazwa nie istnieje) → `parcel_lane_merge.run` (`:112`)/`main` (`:154`) czyta `orders_state.parcels_shadow.json` (pisany przez sidecar panelu) i **emituje NEW_ORDER** (`:127`) → paczki przechodzą ten sam `shadow_dispatcher`/`assess_order` co gastro. Przydział paczki w konsoli osobną ścieżką `assign_parcel` (`assign.py:161` → `dispatch_v2.parcel_assign`), nie przez gastro.

**4 handlery recanon (zdarzenia worka)** w `panel_watcher.py`, wołane z `_diff_and_emit`:
- **assign** — `_save_plan_on_assign_signal` `:641` → recanon(assign) `:654` + redecide `:663` (woł. `:1427/:1540/:1572/:1684/:2397`).
- **deliver** — `_advance_plan_on_deliver` `:668` → `advance_plan` + recanon(deliver) `:698` (woł. `:1479/:1815/:1960`).
- **return/cancel** — `_remove_stops_on_return` `:703` → `remove_stops` + recanon(return) `:726` (woł. `:1980`).
- **pickup** — `_update_plan_on_picked_up` `:731` → recanon(pickup) `:759` + redecide `:760` (woł. `:2071/:2132`).

Zasada (enrichment §C, zweryfikowana strukturalnie): `recanon_courier` NIE przycina (retime-only) — tranzycje kurczące worek (deliver/return/cancel) MUSZĄ najpierw `advance_plan`/`remove_stops`. Assign+pickup dodatkowo wołają `redecide`; deliver+return nie (celowo).

---

## (d) NIEPEWNOŚCI / HIPOTEZY / SPROSTOWANIA DRYFU

1. **Efektywne wartości flag runtime = tylko HIPOTEZY.** Podane defaulty to wartości z PLIKÓW (`config.py`, module-const), nie z żywego procesu. Flagi env-frozen (`plan_recheck` route/canon, `APP_ROUTE_FROM_CONSOLE`, `FROZEN_PICKUP_ETA`) rozstrzyga env procesu. **Weryfikacja:** `systemctl show <unit> -p Environment` + `flags.json` dla silnika. NIE robiłem tego (poza zakresem read-only allow-listy).

2. **`APP_ROUTE_FROM_CONSOLE` default „0" vs enrichment „=1 short-circuituje".** Rozjazd: `courier_api/config.py:68` ma default „0", a enrichment §E twierdzi że LIVE `=1` skraca ścieżkę przez `route_podjazdy` (`_console_done=True`). **HIPOTEZA:** wartość LIVE nadpisana env/drop-inem na „1". Zweryfikować `systemctl show courier-api.service -p Environment | grep APP_ROUTE`. Wpływ: kolejność stopów apki (krok 6c).

3. **Rozjazd apka↔konsola (enrichment #11, P2).** `route_podjazdy.order_podjazdy` (apka) to RÓWNOLEGŁA re-implementacja `fleet_state._build_route` (konsola), nie delegacja (docstring „deleguje" fałszywy). Nie weryfikowałem parytetu na żywym logu `ziomek_time_route_monitor.jsonl` (poza repo). Kolejność stopów żyje w 3 kopiach: silnik `plan_recheck._apply_canon_order_invariants` + apka `route_podjazdy` + konsola `fleet_state._build_route`.

4. **SPROSTOWANIE dryfu vs enrichment §E:** `pending_proposals.json` NIE używa już współdzielonego `{path}.tmp`. HEAD `pending_proposals_store.py:88` używa unikalnego `tempfile.mkstemp` pod `_locked` (`:50`). Uwaga enrichment o „telegram+store SHARE {path}.tmp" jest NIEAKTUALNA dla ścieżki store; ryzyko dotyczy tylko re-enable Telegrama.

5. **`_serialize_result` — pojedynczy zapis w pętli głównej** (FAKT: `:1240` w `_tick`, `_append_decision` `:1348`). Jedyny writer `shadow_decisions.jsonl`. NIE weryfikowałem, które rodziny metryk realnie lądują w logu (enrichment §D #16: część HARD ginie — wymaga grepu żywego `logs/shadow_decisions.jsonl`, poza zakresem).

6. **Granica konsoli = osobne repo** `nadajesz_clone/panel` (backend `nadajesz-panel.service` :8000, DB `nadajesz_panel@:5433`). Importuje `dispatch_v2` jako bibliotekę. `assign.py`/`fleet_state.py` zweryfikowane po ścieżce; endpoint HTTP routera FastAPI („1-klik") NIE dotrasowany — `assign_courier` (`:63`) to warstwa serwisowa; **HIPOTEZA:** wołana z routera w `…/panel/backend/app/` (którego — nie sprawdzałem).

7. **`gastro_assign.py` lokalizacja.** Żywy skrypt = `scripts/gastro_assign.py` (korzeń scripts, NIE `dispatch_v2/`). Konsola wskazuje go przez `settings.ziomek_assign_script`. **HIPOTEZA:** `settings` wskazuje właśnie ten plik; nie odczytałem configu panelu. Zweryfikować grepem `ziomek_assign_script` w konfigu panelu.

8. **Procesy dla recanon — DWA env.** `recanon_courier`/`redecide_courier` z `panel_watcher` biegną pod env `dispatch-panel-watcher`; ten sam `plan_recheck` w cyklu 5-min pod env `dispatch-plan-recheck`. Flagi route/canon są module-const env-frozen — muszą być zmirrorowane w OBU drop-inach, inaczej ta sama funkcja zachowuje się różnie per proces (enrichment §C, parytetu drop-inów NIE weryfikowałem — poza zakresem).

9. **Nie weryfikowałem** stanu aktywności serwisów (`is-active`), zawartości `flags.json`, ani żywych logów — zgodnie z zakazem. Wszystkie „LIVE/OFF" pochodzą z komentarzy kodu/enrichment i wymagają potwierdzenia runtime przed jakąkolwiek zmianą.

10. **Numery linii dryfują** (repo mutuje na żywo, auto-push co godzinę cronem wg `docs/ARCHITECTURE.md §4`). Wszystkie cytowane linie ważne na HEAD `fcf1342` — przy późniejszej pracy grepuj symbol, nie ufaj linii.
