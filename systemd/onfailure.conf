# Drop-in: Telegram alert gdy tick monitora padnie (wzór dispatch-cod-panel-ingest).
# Cel: jeśli sam monitor się wywróci (np. corrupt log), Adrian dostaje sygnał —
# bo cichy monitor = brak nadzoru nad trialem B3.
[Unit]
OnFailure=dispatch-onfailure-alert@%n.service
