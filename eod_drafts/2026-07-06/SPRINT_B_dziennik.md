# SPRINT B (Pakiet 3+4 bez K15: K14 · K16 · K17) — dziennik

Sesja B (tmux 23), start 06.07 ~15:05 UTC. Spec: `docs/refaktor/06-sprint-rownolegly-B-pakiet3-4.md`. Worktree: `wt-sprintB` (+ pkgroot `wt-sprintB-pkgroot`). Równolegle: SESJA A (tmux 22, Pakiet 2).

## PROŚBY DO A
- **[15:20] Rejestracja flagi w ETAP4_DECISION_FLAGS (common.py — Twoja wyłączność):** proszę dopisz przy okazji najbliższego commita w common.py:
  - `ENABLE_POS_SOURCE_HIERARCHY` (K16; stała-fallback False; konsument: courier_resolver.py przez `C.flag(name, False)`)
  Do czasu rejestracji konsumuję literalny default False — kod bez klucza w flags.json = OFF = 1:1.
  - ~~`ENABLE_PLAN_RECHECK_FEAS_GATE` (K14)~~ **WYCOFANE 15:25** — K14 już istnieje jako LIVE `ENABLE_PLAN_RECHECK_GATES` (werdykt niżej); druga flaga = wzorzec #2.

## INFO DLA A (nie wymaga działania, jeśli już to robisz)
- **[15:12] mypy-ratchet: baseline „0" = VOID (kłamiący przyrząd, klasa C9).** `run_mypy` w `tools/devlint/ratchet_check.py` nie sprawdza returncode; mypy w katalogu z myślnikiem w nazwie (`wt-*` — każdy worktree!) wywala się na starcie („contains __init__.py but is not a valid Python package name") → 0 linii `: error:` → baseline K01 zapisał mypy=0. Prawda z KANONU (świeży cache, pełny zakres MYPY_MODULES): **163 błędy** — stan zastany, zero przyrostu od ca525c7. Widzę Twój worktree `wt-k01check` → zakładam, że fix bierzesz Ty (fail-loud na crash + baseline 163 albo bieg przez pkgroot). Ja do tego czasu bramkuję się ruff 608/608 + pełną suitą; mypy odnotowuję informacyjnie.

## ETAP 0 (protokół #0) — stan na żywo 06.07 15:03-15:12 UTC ✅
- Multi-sesja recon: tmux 21 (koordynator), 22 (SESJA A, Pakiet 2), 23 (ja). Worktrees: kanon@master `3f4ed26`, `wt-refaktor-arch` (docs), `wt-k01check` (A, diagnoza ratcheta). Working tree kanonu czysty poza untracked eod_drafts (md/json — cudze raporty).
- Flagi programu w flags.json: `ENABLE_WORLD_RECORD`/`ENABLE_FLAG_SNAPSHOT`/`ENABLE_PRE_RECHECK_BEFORE_POOL`/`ENABLE_EFFECTS_AFTER_DECISION` = **true (4/4 LIVE)** — zgodnie z 05-dziennik (flipy za jawnym TAK Adriana 14:02/14:48). `ENABLE_PLAN_RECHECK_FEAS_GATE`/`ENABLE_POS_SOURCE_HIERARCHY` = brak klucza (OFF), zgodnie z planem.
- Serwisy: dispatch-shadow active, dispatch-plan-recheck.timer active, dispatch-panel-watcher active. `dispatch-night-guard.timer` istnieje (daily 01:15 UTC) — punkt wpięcia K17c.
- atq: tylko at-208 19:30 (raport bundle_calib — czysty per 05-dziennik/rejestr; nie flipuje flag).
- Korpus `world_record-20260706.jsonl`: 16 rekordów, **7 z now≠null** (od 14:48, po restarcie K06a). Parytet ≥30 decyzji → wykonalny po peaku 17-20 / rano 07.07. Buduję K14/K16 teraz, biegi parytetowe na pełnym oknie.
- **Testy bazowe KANON: 4294 passed / 0 failed** (26 skipped, 10 xfailed; 121,6 s) = baseline spec ✓. Ruff ratchet 608/608 ✓. Mypy — patrz INFO DLA A (void baseline, nie moja zmiana).
- Worktree `wt-sprintB` utworzony z master `3f4ed26`, gałąź `refaktor/krok-14-reseq-gate`; pkgroot z symlinkami `dispatch_v2`/`flags.json`/`logs`.

## K14 — WERDYKT: JUŻ WYKONANE przez falę L3 (klasa C15), zero nowego kodu ✅ (15:25)
**Bramka „re-plan nie pogarsza R6" istnieje i jest LIVE od 02.07:** commit `dae7b2a` (fala L3, F2/K2) — compare-and-keep na ZAPISIE regenu w `_gen_one_bag_plan` (plan_recheck.py:889-930; komentarz :1017-1031, dokładnie ten, który spec każe „odtworzyć"). Flaga `ENABLE_PLAN_RECHECK_GATES` = **true w flags.json** (enforce, nie shadow — measure-first skonsumowany przez tamtą falę).
**Dowody (liczby, nie lektura — C4/C15):**
- Testy: `tests/test_l3_plan_recheck_gates.py` 24 testy, w tym behawioralne ON≠OFF (`test_gate_off_saves_byte_for_byte`, `test_gate_on_rejects_breaking_regen_over_clean_existing`) + 2 mutation-testy (C13) + case ze spec („fresh łamie R6, existing czysty → REJECT").
- Żywy monitor (log `plan_recheck.log`, summary per tick od 02.07): `l3_regen_pass` w setkach ticków, `l3_regen_both_breach` ~100 ticków, `l3_regen_no_baseline` ~50, **`l3_regen_rejected`: 1 tick** → bramka NIE zamraża planów masowo (kryterium 04-planu spełnione empirycznie).
- q2_drift (ziomek_time_route_monitor): przed flipem 26.06-01.07 śr. ~20/d (5-31), po 03-05.07 śr. ~15/d (13-18) — kierunek zgodny, statystycznie słaby (szum; przy 1 odrzucie bramki oczekiwany wpływ ~0; jej wartość = bezpiecznik „nie-cofa").
**Mapa kompletności ścieżek zapisu planu:** (1) `_gen_one_bag_plan` (jedyna PERMUTUJĄCA — `_sweep`) → za bramką L3 ✅; (2) `_retime_one_bag_plan` (save:1863) → NIE permutuje (retime + deterministyczne niezmienniki kanonu z własnymi flagami/guardami LIVE — poza definicją K14); (3) `panel_watcher._save_plan_on_assign`/`insert_stop_optimal` → ścieżka przypisania (sekwencja z pełnej feasibility silnika), poza plan_recheck.
**Świadome N-D:** (a) NIE tworzę flagi `ENABLE_PLAN_RECHECK_FEAS_GATE` — byłaby DRUGĄ flagą tej samej bramki (wzorzec #2); prośba do A o rejestrację wycofana dla K14. (b) Metryka zostaje w summary (log tekstowy), nazwa `l3_regen_rejected` zamiast `would_reject_reseq` — semantyka pokryta, bramka już enforce więc „would_" bezprzedmiotowe. (c) Parytet korpusowy OFF: trywialny — zero zmian kodu w K14.

## K17a + K17c — runner bramki korpusowej + night-guard (commit `adf0fae`, merge do master ~15:30) ✅
- **`tools/world_replay_gate.py`**: iteracja po world_record `now≠null` (okno --since/--hours/--until, dedup, pomija now=null z licznikiem), replay przez `world_replay.replay_one` (reużycie 1:1), join z shadow **rotation-aware** (`ledger_io.iter_shadow_decisions`, C16), klasyfikacja różnic **krytyczne** (verdict/best_cid/best_score) vs **miękkie** (reason/pool_*), raport → `dispatch_state/world_replay_gate_verdict.txt` (zapis atomowy) + exit 0=PARITY / 1=DIFFS / 2=EMPTY_WINDOW. 8 testów (`test_world_replay_gate_k17.py`), replay mockowany + assert anty-prod C17.
- **Utwardzenie `world_replay` (znalezione BRAMKĄ, nie lekturą):** sandbox robił **żywy fetch panelu** (`get_fresh_czas_kuriera_for_bag` — K07 prefetch i legacy per-kandydat) = sieć w replayu + ck świeższe niż nagranie. Stub `{}` w sandboxie. Dowód złapania: smoke 15:20 pokazał `pre_proposal_recheck oid=485888 Δ=+10min` W TRAKCIE replayu.
- **Smoke na żywym korpusie (n=13):** 12 zgodnych, **1 różnica MIĘKKA** (485919: pool_feasible 6 vs 5, verdict/best/score IDENTYCZNE; deterministyczna 2×). Diagnoza: kandydat 541 na granicy R6 picked_up delta-reject (43,5 min) — flip werdyktu feasibility od ŻYWEGO pliku kalibracyjno-stanowego nienagranego w world_record v0 (v0 nagrywa flags+fleet+osrm+now; NIE nagrywa m.in. orders_state/reliability-feed/loadgov). **Ograniczenie wierności v0, nie wada bramki** — stąd klasyfikacja krytyczne/miękkie w raporcie.
- **K17c night-guard:** NOWA para unitów `systemd/dispatch-world-replay-gate.{service,timer}` — 02:00 UTC (po night-guard 01:15, przed rotacją 03:00/restic 03:30), **TRYB INFORMACYJNY** = `ExecStart=-…` (exit ignorowany, zero OnFailure/Telegram); instrukcja eskalacji na egzekwujący (usuń „-", dodaj OnFailure) w komentarzu unitu. **NIE zainstalowane — instalacja wyłącznie za jawnym TAK Adriana** (komenda w komentarzu unitu).
- Weryfikacja: worktree pełna regresja **4302/0** (baseline 4294 + 8 nowych) · rebase na `ec4a1b5` (devlint-fix A) czysty · ratchet z kanonu po merge **ZIELONY ruff 608/608 · mypy 124/124** · pełna suita kanoniczna po merge w toku (wynik niżej).

## K16 — hierarchia źródeł pozycji: NA MASTERZE (commit `bab1797`, merge+push ~15:45) ✅
- **Rekonesans (subagent read-only):** pełna mapa writerów/readerów gps/pwa/last_pos + 8 ryzyk (m.in. hierarchia zakodowana strukturą `continue`; bliźniaczy, węższy czytnik GPS w plan_recheck bez bbox/quality-guardów; syntetyki `pre_shift`/`post_shift`/`working_override` nadawane w `dispatchable_fleet` PO snapshocie; 8 bliźniaków klasy no-GPS potwierdzone). Raport w transkrypcie sesji; ryzyka zaadresowane wyborem wariantu poniżej.
- **Wariant wykonania (świadome odstępstwo od „OFF=stara ścieżka"):** zamiast DRUGIEJ kopii hierarchii za flagą (wzorzec #2) — **przenosiny 1:1** bloku kroków 1-4+4a (linie 919-1136) do `_resolve_position(...)` = JEDNO miejsce łączenia źródeł; **dowód bajt-parytetu przenosin skryptem** (blok stary ≡ nowy poza 4 `continue`→`return`; kompilator = siatka na pominięte). Flaga `ENABLE_POS_SOURCE_HIERARCHY` bramkuje WYŁĄCZNIE addytywną adnotację `pos_resolution` (`PositionResolution(known, source, from_store)` + `is_position_known()` = jedyne miejsce klasyfikacji Known|Unknown, F-3 minimalnie). Adnotacja = atrybut DYNAMICZNY (nie pole dataclassy) → OFF: zero nowych pól/bajtów; ON: nie zmienia pos_source ani serializacji world_record (iteruje dataclasses.fields — zweryfikowane w kodzie).
- **Klasa „równe traktowanie no-GPS" NIETKNIĘTA:** emitowane stringi pos_source identyczne co do znaku; test ON≠OFF asertuje równość (pos, pos_source, age, from_store) całego snapshotu przy obu stanach flagi.
- **Dowody:** 10 testów `test_pos_hierarchy_k16.py` (7 charakteryzujących gałęzie — GREEN na kodzie SPRZED zmiany; OFF-bez-adnotacji; ON≠OFF z parytetem pozycji; klasyfikacja Known|Unknown) · regresja worktree **4312/0** · kanon po merge **4312/0** · ratchet ZIELONY (ruff 608, poniżej baseline) · py_compile.
- **Świadome N-D:** (a) bliźniaczy czytnik pozycji w `plan_recheck._start_anchor` (własna, węższa hierarchia — TYLKO PWA, bez guardów) zostaje — unifikacja = K15/WorldState (odnotowane dla scalenia); (b) re-rezolucja pozycji w `_reconstruct_bag_from_panel_packs` zostaje inline (osobny blok enrich); (c) syntetyki dispatchable_fleet poza resolverem (nadawane po snapshocie) — `POSITION_UNKNOWN_SOURCES` już je klasyfikuje na przyszłość. (d) Parytet korpusowy OFF → bieg bramki K17b (kod post-K16 przy OFF vs zapisy żywego procesu sprzed K16 = dokładnie ten dowód), wynik niżej.

### PROŚBA DO A / backlog programu (world_record v1)
Dograć do nagrania: wynik prefetchu `czas_kuriera` (K07), slice orders_state/loadgov/reliability użyty w decyzji — usunie klasę różnic miękkich replayu (pool-drift od żywych plików). Pliki: `world_record.py`/`dispatch_pipeline.py` (Twoje).

## K17b — RAPORT PARYTETU z pełnego peaku 06.07 17-20 (at-210 odpalony 18:20, werdykt odczytany 18:2x) ✅ = FORMALNA BRAMKA K06
**Liczby (okno 14:48→18:20, pre-peak + PEŁNY peak):** n=**74** decyzje z now≠null · **zgodne CO DO ZNAKU 64/74 (86,5%)** · **0 missów OSRM · 0 braków zapisu · 0 błędów** (join 100%, zegar wierny) · różnice 10 = 7 „krytycznych" + 3 miękkie. Werdykt: `dispatch_state/world_replay_gate_verdict.txt`.
**Anatomia 10 różnic (każda przejrzana):**
- **6/7 „krytycznych" = wyłącznie dryf `best_score` przy TYM SAMYM zwycięzcy i werdykcie** (Δ 7,6-40 pkt; 485920/485965/485966/485967/485979/485980) — komponenty score czułe na ŻYWE wejścia spoza nagrania v0 (głównie: replay stubuje żywy prefetch `czas_kuriera` [utwardzenie K17a — żywa decyzja używała fetchu], + żywe pliki stanu). `courier_reliability.json` (mtime 04:30) i `courier_tiers.json` (10:48) NIEZMIENIONE w oknie — wykluczone jako przyczyna.
- **3 miękkie = pool_feasible ±1** przy identycznym zwycięzcy (485919/485932/485941) — ta sama klasa v0; 485919 miał IDENTYCZNĄ różnicę w smoke PRZED merge K16 → refaktor nie jest źródłem.
- **⭐ 485927 = JEDYNA różnica zmieniająca DECYZJĘ** (best 484 vs 179, score 80,3 vs −54,9, pool 5 vs 6): zwycięzca live (179) wypadł z puli w replayu — skrajny przypadek tej samej klasy pool-drift, ale JEDYNY do realnej diagnozy przy world_record v1 (nagranie brakujących wejść pokaże, który plik flipnął feasibility 179).
**Werdykt klasy:** różnice = **luka NAGRYWANIA v0** (żywe wejścia poza rekordem: wynik prefetchu ck, orders_state/loadgov, inne pliki stanu), **NIE regres silnika** — żywy proces cały dzień biegł na kodzie sprzed K16/K17 (bez restartu), replay kodem post-merge daje 64/74 zgodności co do 3 miejsc po przecinku, a każdy mismatch angażuje komponent czuły na żywe wejście. Rekomendacja: **world_record v1** (dogranie wejść — PROŚBA DO A wyżej) → oczekiwany parytet 74/74; do tego czasu night-guard zostaje **INFORMACYJNY** (decyzja koordynatora zgodna z planem).
**Doprecyzowanie zakresu dowodu K16 (uczciwość C9/C10):** ścieżka replayu NIE woła `build_fleet_snapshot` (flota rehydrowana z nagrania) → bieg korpusowy jest dla K16 dowodem pośrednim (import+moduł); twardy parytet-OFF K16 = 7 testów charakteryzujących gałęzie + bajt-dowód przenosin bloku + pełna suita 4312/0. Dla K14 (zero zmian kodu) — trywialny.

## ADDENDUM KOORDYNATORA (przyjęte 15:10)
Po spełnieniu kryterium końca sprintu B ostatnią linią tego pliku będzie deterministyczny marker `=== SPRINT_B_KONIEC ===` (koordynator wykrywa → rusza K15). Nie zastępuje STOP-ów dla Adriana.

## KONIEC SPRINTU B — kryteria (18:30 UTC) ✅
- **K14** ✅ werdykt „done-by-L3" (LIVE od 02.07, `dae7b2a`; zero duplikatu; zaakceptowany przez koordynatora).
- **K16** ✅ na masterze (`bab1797`), flaga `ENABLE_POS_SOURCE_HIERARCHY` OFF + zarejestrowana w ETAP4 przez sesję A (`c715279`); dowody ON≠OFF + parytet-OFF (7 testów charakteryzujących + bajt-dowód przenosin; korpus pośrednio — replay nie woła build_fleet_snapshot).
- **K17** ✅ runner na masterze (`adf0fae`) · **raport parytetu z PEŁNEGO peaku** (n=74: 64 zgodne, 0 missów; klasa różnic = luka nagrywania v0; ⭐485927 jedyna zmiana decyzji → diagnoza przy v1) · night-guard PRZYGOTOWANY w `systemd/` — tryb INFORMACYJNY do czasu world_record v1 + zielonej bramki; **instalacja wyłącznie za jawnym TAK Adriana**.
- Kanon **4325 passed / 0 failed** (baseline ruchomy — zawiera K09/K10 sesji A; 2 xpassed z cudzych xfaili, nie moje pliki) · ratchet ZIELONY (ruff <608, mypy 124 baseline po fixie A).
- Zero flipów / restartów / instalacji unitów przez całą sesję; at-210 = czysty raport, DONE w [[shadow-jobs-registry]].
- Pending PO sprincie (dla programu): world_record v1 (prośba do A w sekcji wyżej) → re-bieg bramki → oczekiwane 74/74 → dopiero wtedy rozmowa o eskalacji night-guard na egzekwujący (3 zielone noce + decyzja Adriana). K15 (wspólny Planner) = punkt scalenia po Pakietach 2 i 3, poza tym sprintem.

=== SPRINT_B_KONIEC ===
