# SPRINT RÓWNOLEGŁY — SESJA A (Pakiet 2: K09-K13) — dziennik

Sesja: tmux 22 · start 06.07.2026 ~15:00 UTC · spec: `docs/refaktor/06-sprint-rownolegly-A-pakiet2.md` · worktree: `/root/.openclaw/workspace/scripts/wt-sprintA` (+ pkgroot `wt-sprintA-pkgroot` z symlinkami dispatch_v2/flags.json/logs).

## KOMUNIKATY DO B / KOORDYNATORA
- **[19:40] ⚠ INTERPRETACJA at-210 (7 krytycznych na n=74) — DIAGNOZA WYKONANA, kod NIEWINNY, klasa = dryf żywych plików:** eksperyment kontrolowany 19:33 (okno zamrożone ≤18:20, biegi minutę po sobie): gate na masterze (z K09+K10) ≡ gate na `bab1797` (sprzed K09/K10) **co do znaku** (te same 13 różnic: 10 kryt + 3 miękkie). Liczba różnic ROŚNIE z czasem od decyzji przy NIEZMIENIONYM kodzie i zamrożonym oknie (16:37→1 kryt · 18:20→7 · 19:33→10) — bo replay czyta ŻYWE `courier_plans`/`orders_state`/reliability (inner: ENABLE_SAVED_PLANS_READ itd.). **Wniosek do bramki K06/K17: różnice KRYTYCZNE też powstają z dryfu v0 (nie tylko miękkie) — sama bramka bez kontroli nie orzeka o kodzie.** Poprawna metodologia do czasu world_record v1: bieg KANDYDAT + bieg KONTROLNY (kod-referencyjny) na tym samym oknie back-to-back → kryterium = raporty identyczne (tak zrobione dla K09 i K10). Night-guard 02:00 (K17c) replayuje ~7-26h po decyzjach → spodziewany wysoki szum krytycznych; werdykt night-guard musi porównywać do kontrolnego biegu albo czekać na v1. Dowody: `scratchpad/gate_{master,bab1797}_1932.txt` + `gate_{k09,k10,master}.json` (sesja A).
- **Przejąłem `tools/devlint/` (nieprzypisane w tabeli własności)** — patrz wpis „Naprawa przyrządu devlint" niżej. B: NIE edytuj `tools/devlint/*`; po `git merge` z mastera Twój ratchet ma baseline mypy=124 (nie 0).
- Push `origin master` wstrzymany przez klasyfikator uprawnień harnessu (odmowa push na default branch) — merge'y są w LOKALNYM masterze; push do wykonania przy STOP-ie za wiedzą Adriana.

## ETAP 0 — stan na żywo (06.07 15:00-15:15 UTC) ✅
- Master kanon `3f4ed26` = zgodny ze spec; working tree czysty (tylko cudze untracked .md/.json w eod_drafts).
- 4 flagi programu w flags.json = **true** (WR/K05/K07/K08) ✓; dispatch-shadow active, journal 0 błędów od restartu 14:48.
- Korpus `world_record-20260706.jsonl`: 15 rekordów, **6 z now≠null** (od 14:49) — za mało na parytet ≥30; zbierze się w peaku 17-20 PL. Deweloperka K09 idzie równolegle, krok 3 rytmu (parytet) wykonam gdy korpus urośnie.
- Testy bazowe KANON: **4294 passed / 0 failed** (26 skipped, 10 xfailed) — baseline potwierdzony.
- Multi-sesja recon (C1): tmux 21=koordynator, 22=ja (A), 23=B (etap 0), 11=audyty (inny obszar), 20=zombie. B nie ma jeszcze worktree ani dziennika. atq: tylko at-208 19:30 = czysty raport (nie flip).

## Naprawa przyrządu devlint (PRZED K09) — commit `ec4a1b5`, merge do master ✅
- **Near-miss klasy #17 (kłamiący przyrząd):** `ratchet_check.run_mypy` w worktree z myślnikiem w nazwie (`wt-refaktor-arch`, `wt-sprintA`) — mypy padał usage-errorem `"...contains __init__.py but is not a valid Python package name"` ZANIM cokolwiek sprawdził (wspinaczka po `__init__.py` do katalogu o niepakietowej nazwie), a licznik zliczał 0 linii `: error:` = **fałszywe ZIELONO**. Baseline K01 „mypy 0" mierzony w `wt-refaktor-arch` = artefakt awarii narzędzia.
- **Dowody:** bisekcja K01→master (95d0ff3, f31fe86, a26cc5c, 4719207, a284bac, 8eb499d, 1c3702a, 98ac075, 3f4ed26) — ŻADEN commit refaktoru nie dodał błędów; realna liczba na 19 modułach rdzenia istniała od zawsze. Kanon starym wywołaniem: 163; nowym (nazwy modułów od korzenia repo): **124 = identycznie w kanonie i w worktree** (parytet przyrządu zweryfikowany fresh-cache).
- **Fix u źródła:** (a) rc mypy poza {0,1} albo rc=1 bez linii błędów → **exit 2 awaria narzędzia** (koniec liczenia 0 z awarii); (b) `cwd=REPO` + ścieżki względne + `explicit_package_bases`+`namespace_packages` w mypy.ini → deterministyczne nazwy modułów niezależne od nazwy katalogu; (c) uczciwy re-baseline **mypy=124** (ratchet „nie gorzej" od prawdziwej liczby; zapadka zacznie realnie chronić core/ od K09).
- Weryfikacja: ratchet ZIELONO w wt-sprintA i w kanonie po merge (608/608 ruff · 124/124 mypy); py_compile OK; **pełna suita kanoniczna po merge: 4294/0** ✅.

## K09 — fasada core.decide + WorldState (commit `572808b` na `refaktor/krok-09-decide`; merge po parytecie korpusowym)
- **Co:** NOWE `core/decide.py` (fasada `decide(world, order)` = czysta delegacja 1:1 do publicznego `dispatch_pipeline.assess_order` — wrapper K08+observability nietknięty; wywołanie przez atrybut modułu → monkeypatch w testach działa przez fasadę) + `core/world_state.py` (`WorldState` = pola 1:1 z parametrami assess_order; `_bypass_early_bird` świadomie kwargiem fasady, nie polem świata — to tryb pytania, nie stan świata). Call-site'y przez fasadę: `shadow_dispatcher.process_event` (JEDYNA zmiana w pliku, zgodnie z tabelą własności), `czasowka_scheduler`, `postpone_sweeper`, `replay_failed`, tools/{sequential_replay, perf_lazy_harness, pending_global_resweep, replay_feasibility, reassignment_forward_shadow}. **`tools/world_replay.py` = świadome N-D** (własność B; fasada przezroczysta, parytet nietknięty — migracja u właściciela).
- **Mapa kompletności symbolu (klasa „sygnatura/atrybut ma konsumentów w testach", jak K06a):** 7 plików testów patchowało `assess_order` na modułach-callerach → przestawione na jeden kanoniczny cel mocka `dispatch_pipeline.assess_order` (test_now_explicit_k06a, test_czasowka_proactive_evaluator, test_postpone_auto_replan, test_reassignment_forward_shadow_2026_06_22, test_czasowka_dispatchable_fleet_fix, test_czasowki_fixes_2026_05_07, test_postpone_sweeper_schema_k02). NOWY strażnik AST (`test_decide_facade_k09`): nikt poza allowlistą {dispatch_pipeline, core/decide, tools/world_replay} nie WOŁA assess_order (ast.Call, nie grep — odporny na docstringi).
- **Fix u źródła po drodze:** `tools/sequential_replay.py` hardcodował sys.path na kanon → subprocess-testy (test_sequential_replay_verdict_direction_l78) w biegu worktree importowały STARY pakiet bez core/ → env-first `ZIOMEK_SCRIPTS_ROOT` (wzorzec perf_lazy_harness). devlint: +`core/{decide,world_state}.py` do MYPY_MODULES (mypy-czyste — zapadka zaczyna chronić core/).
- **Weryfikacja (worktree):** pełna regresja **4298/0** (baseline 4294 + 4 nowe testy K09) · ratchet 608/608 ruff, 124/124 mypy · py_compile OK.
- **Rebase na master `bab1797`** (K16+K17 sesji B weszły w trakcie) — czysty, pliki rozłączne; K09 po rebase = `92cd23c`. Regresja na szczycie łańcucha po rebase: **4325/0**.
- **PARYTET KORPUSOWY (bramka `world_replay_gate` sesji B, okno since=14:48, n=30, ≥30 ✓):** replay kodem K09 (pin pkgroot) = **27/30 zgodne co do znaku, 3 różnice (1 krytyczna 485927, 2 miękkie 485919/485932), 0 missów** — i bieg KONTROLNY na kodzie MASTERA daje **IDENTYCZNY raport co do znaku** (diff obu raportów pusty poza timestampem). Wniosek: wszystkie 3 różnice = znana klasa „żywe pliki poza nagraniem v0" (courier_plans/orders_state dryfują od czasu decyzji; opisana przez B w K17a), **K09 wnosi 0 różnic**. To jest dowód „bez zmiany zachowania" przy niedoskonałym przyrządzie: kryterium = zbiór różnic identyczny z kontrolą, nie absolutne zero (absolutne zero na tym oknie jest nieosiągalne także dla samego mastera).
- **MERGE do mastera + pełna suita kanoniczna: 4316/0** · ratchet ZIELONY. ✅ K09 ZAMKNIĘTY.

## K10 — bramki wejściowe → core/gates.py [PRZENOSINY 1:1] (commity `7b08789`+`c715279`, merge do master ~16:45) ✅
- **Co:** `core/gates.py` — `geocode_defense` (SKIP/no_pickup_geocode, L2) + `early_bird` (KOORD, próg z RAW `pickup_at_warsaw` fix 2026-05-07, rekurencyjny kontrfaktyk EARLYBIRD-01 głębokości 1) przeniesione z `_assess_order_impl` treścią 1:1 (warunki, komunikaty logów, kształt PipelineResult, kolejność efektów; kolejność LOADGOV→geokod→parsowanie pickup_at→early-bird bez zmian; współdzielony `pickup_at` ZOSTAJE w impl — per rekonesans read-only agenta). Helpery early-bird zostają w dispatch_pipeline (konsument zewnętrzny: `shadow_dispatcher` importuje `_early_bird_threshold_min`) — bramki wołają je przez moduł (`_dp.*`, lazy w funkcji — bez cyklu importów; monkeypatch istniejących testów działa bez zmian).
- **Mapa kompletności strażników źródła (klasa C13 anti-teatr):** `test_verdict_gate_guards` skanuje teraz dispatch_pipeline + core/gates.py (inaczej `early_bird` znika z pola widzenia skanera = martwy strażnik); analogicznie `test_scale01_caps_flags` (konsument-helpera) i `test_always_propose…early_bird_NOT_guarded` skanują źródło gates. `tools/guard_mutation_probe` nietknięty (mutuje wyłącznie bramki QUALITY-negated — wszystkie zostały w pipeline). devlint: +core/gates.py do MYPY_MODULES (czysty); 1 celowany `type: ignore[arg-type]` w impl (mypy nie widzi narrowingu przez granicę funkcji; semantyka 1:1).
- **Testy:** 8 nowych `test_gates_k10.py` (SKIP/sentinel/pass, format reason co do znaku, RAW-anchor vs extended, bypass, kontrfaktyk z licznikiem wywołań impl=1, parytet przez wrapper). Naprawa 2 testów źródło-skanujących (wyżej) = kontrakt przeniesiony, nie osłabiony.
- **PARYTET KORPUSOWY:** bramka na kodzie K10 = **raport IDENTYCZNY co do znaku z kontrolą master** (n=30; diff pusty poza timestampem) → K10 wnosi 0 różnic.
- **MERGE do mastera + pełna suita kanoniczna: 4325/0** · ratchet ZIELONY (ruff poniżej baseline po sprzątkach B). ✅ K10 ZAMKNIĘTY.
- **Delta monolitu po K09+K10:** `_assess_order_impl` **3800 → 3740 linii (−60)**; wyniesione: 2 bramki HARD (138 linii core/gates.py) + fasada wejścia (core/decide.py + world_state.py). Główne cięcia = K11 (pętla ~2147 l.) i K12 (selekcja).

## K11 — pętla per-kurier → core/candidates.py [PRZENOSINY 1:1] (commity `dfeef03`+`de91a20`, merge do master ~20:25) ✅
- **Co:** `eval_courier_inner` = dosłowna treść dawnej closure `_v327_eval_courier_inner` (~2147 l., ciało bajt-w-bajt) + `eval_courier` (wrapper TLS OSRM 1:1). 18 odczytów z closure → jawny `EvalContext` (prolog odpakowuje do lokalnych nazw 1:1; budowany w impl PO `_k07_prefetched_ck` — closure czytała tę nazwę late-bound). Symbole module-level pipeline'u związane w prologu aliasami `X = _dp.X` (odczyt atrybutu per-call) → **każdy kontrakt monkeypatch na atrybutach dispatch_pipeline obowiązuje przez core** (tools/replay_feasibility: check_feasibility_v2; testy K07: get_fresh_czas_kuriera_for_bag; wspólny logger = treść logów 1:1). Pula/executor/`_v328_eval_safe`/mass-fail heuristic ZOSTAJĄ w impl (domena K12; mass-fail buduje Candidate bliźniaczo — w mapie K12). **Monolit `_assess_order_impl`: 3740 → 1598 linii.**
- **Odstępstwo metodyczne (odnotowane):** pierwszy wariant = token-rewrite `X→_dp.X` w ciele — ZŁAMAŁ wzorce 26 testów-skanerów źródła (exact-match treści pętli). Wycofany na rzecz aliasów prologu (ciało bajt-w-bajt) — skanery i kontrakty przeżywają bez osłabień.
- **Mapa kompletności skanerów źródła (klasa C13):** 18 plików testów czytających źródło pipeline pod kątem treści pętli → skan OBU źródeł (asercje negatywne zweryfikowane przed zmianą: 0 trafień w obu plikach — brak odwróceń). W tym `test_intra_restaurant_gap` miał **hardkod kanonicznej ścieżki** = kłamiący strażnik w biegach z worktree (czytał cudzy plik; fałszywie zielony pre-merge, uczciwie czerwony po merge) → self-located + skan obu (`de91a20`, klasa #17).
- **Porządki importów pipeline:** osierocone po przenosinach `scoring`/`eta_load_aware`/`CourierBagState` usunięte (zero konsumentów atrybutu — grep); `DWELL_PICKup…/pipeline_geometry` zostają z noqa (kontrakt `_dp.` + tożsamość test_pipeline_geometry). `candidates.py` świadomie POZA MYPY_MODULES (ciało nietypowane; po typizacji). mypy 124→**122** (2 błędy wyprowadzone z pipeline).
- **Testy:** 4 nowe kontraktowe (`test_candidates_k11`): monkeypatch-through-core ×2, TLS-finally, smoke e2e przez assess_order. XPASS ×2 (`test_obj_food_age_bug5`) = zależne od pory dnia, występują TAK SAMO na czystym masterze — nie-K11 (zweryfikowane biegiem na kanonie).
- **PARYTET (replay różnicowy per dyspozycja koordynatora):** zamrożone okno 14:48-20:00, n=88 — raport gałęzi **identyczny co do znaku** z raportem mastera (12 kryt + 3 miękkie = dryf żywych plików, znosi się po obu stronach; diff pusty poza timestampem).
- **MERGE + pełna suita kanoniczna: 4329/0** · ratchet ZIELONY (poniżej baseline). ✅ K11 ZAMKNIĘTY.

## Zrobione dla B po drodze
- Rejestracja `ENABLE_POS_SOURCE_HIERARCHY` (ETAP4 + stała-fallback False + wiersz LOGIC_REF) — commit `c715279` (prośba B z dziennika, K16).
- Backlog od B przyjęty do STOP-raportu: world_record v1 (dograć wynik prefetchu K07 + slice orders_state/loadgov/reliability — usunie klasę różnic miękkich replayu; pliki moje: world_record.py/dispatch_pipeline.py).

## K12 — selekcja + tiering + best_effort + bramki werdyktu → core/selection.py [PRZENOSINY 1:1] (commit `0045494`, merge ~20:55) ✅
- `select_and_emit` = dosłowna treść końcowego segmentu impl (filtr feasible → kary selekcyjne → sort/tiering → bramki KOORD → best_effort → PipelineResult); 11 odczytów z impl → `SelectionContext`; aliasy prologu `X=_dp.X` (wzorzec K11); ciało bajt-w-bajt. **Monolit `_assess_order_impl`: 1598 → 483 linie** (cel planu „~kilkaset" OSIĄGNIĘTY). Pula+mass-fail zostają w impl.
- **„NOWY re-assert _assert_feasibility_first na EMIT" = WERDYKT C15 (już wykonane):** fala L7.3 wpięła `_split_layer_emit_assert` we wspólny lejek `_classify_and_set_auto_route` (KAŻDY emit; **LIVE** — `ENABLE_SPLIT_LAYER_GUARD=true` w flags.json). Zero nowego kodu bramki (druga kopia = wzorzec #2); dowód utrwalony testem `test_emit_reassert_c15_dowod` (lejek woła re-assert; selection emituje przez lejek ≥8 ścieżek).
- Mapa kompletności skanerów: ~20 plików testów rozszerzone o skan `core/selection.py` (trio; asercje negatywne zweryfikowane); `test_verdict_gate_guards._SRC_PATHS` + selection (bramki KOORD widoczne dla strażnika polaryzacji). PARYTET: replay różnicowy n=88 identyczny co do znaku. Kanon po merge **4332/0**.

## K13 — interfejs Scorer (ADR-R06) za flagą OFF (commit `1b6c80b`, merge ~21:05) ✅
- NOWY `core/scorer.py`: `HeuristicScorer` (tożsamość na final_score → bajt-parytet score z konstrukcji) + `LgbmScorer` (wrapper ISTNIEJĄCEJ inferencji shadow `predict_two_model_for_decision`, fail-soft → heurystyka + **metryka `scorer_fallback`** w metrics = auto-serializacja A+B) + `get_scorer` (`SCORER_IMPL` z flags.json, default heuristic). Wpięcie w `core/candidates.py` tuż przed budową Candidate za `ENABLE_SCORER_INTERFACE` (**ETAP4 + stała-fallback False + wiersz LOGIC_REF; OFF/brak klucza = zero odczytu modułu = 1:1**). Flip LGBM primary = POZA zakresem programu (wyłącznie jawna decyzja Adriana). 5 testów (tożsamość, fail-soft ×2, wybór, toggle realnym mechanizmem flags.json ON==OFF numerycznie + klucze obserwacyjne). Checker flag-effect: czerwony tylko w worktree (hardcode kanonicznych tests/ — checker-krzyż jak K07/K08), **zielony z kanonu po merge (18/18 checkerów flag)**. PARYTET: replay różnicowy n=88 identyczny. Kanon po merge **4337/0**.

## KRYTERIUM KOŃCA SPRINTU A — SPEŁNIONE (06.07 ~21:15 UTC)
- ✅ K09-K13 na masterze (`92cd23c`·`7b08789`+`c715279`·`dfeef03`+`de91a20`·`0045494`·`1b6c80b` + fix przyrządu `ec4a1b5`).
- ✅ `_assess_order_impl` = orkiestrator **483 linie** (z 3800 na starcie; −87%). Nowe moduły: core/{decide,world_state,gates,candidates,selection,scorer}.py.
- ✅ Parytet korpusowy: **0 różnic wnoszonych przez każdy krok** metodą replayu różnicowego (dyspozycja koordynatora): K09 n=30 · K10 n=30 · K11 n=88 · K12 n=88 · K13 n=88 = **324 decyzje parytetowe łącznie (≥100 ✓)** na 88 unikalnych rekordach (=100% korpusu z now≠null; okno 14:48-21:10). Finalny dowód całości: master(po K13) ↔ `bab1797`(sprzed K09) **identyczny co do znaku** na pełnym oknie. Artefakty: `scratchpad/gate_*.txt/json` sesji A.
- ✅ Kanon **4337 passed / 0 failed** (baseline ruchomy 4294→4337; xpassed ×2 = time-of-day, tak samo na masterze pre-sprint). ✅ ratchet ruff ≤608 · mypy 107 (baseline 124; spadek = błędy wyprowadzone z pipeline; candidates.py poza MYPY_MODULES do czasu typizacji — świadome).
- Rezydua/nastepne: **world_record v1** (dyspozycja koordynatora: PO K13 — biorę jako następne zadanie tej sesji; nagranie prefetchu K07 + slice orders_state/loadgov/reliability; aktywacja przy najbliższym restarcie shadow ZA ACK) · flip `ENABLE_SCORER_INTERFACE`/`SCORER_IMPL` = tylko Adrian · push origin/master wstrzymany (klasyfikator uprawnień) · unikalne rekordy korpusu dobiją ≥100 jutro (nocne/poranne zamówienia) — poranny bieg różnicowy master↔bab1797 jako wisienka (opcjonalny, dowód już jest na 100% dostępnego korpusu).

=== SPRINT_A_KONIEC ===
