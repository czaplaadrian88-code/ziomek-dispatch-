# CB-01 (circuit-breaker kuriera) — werdykt: ZAMKNIĘTE jako superseded przez A2 (2026-06-08)

## Pytanie decyzyjne
todo_master: „zaimplementować `courier_recent_delay()` + wpiąć karę, ALBO zamknąć jako
pokryte przez tier late-pickup". Notatka twierdziła „A2 to raport, NIE sygnał scoringu → NIE domyka CB-01".

## Ustalenie: notatka nieaktualna (sprzed flipu A2 06-07)
- `_a2_reliability_soft_score` (dispatch_pipeline.py:886) **LIVE od 06-07** (`ENABLE_A2_RELIABILITY_SOFT_SCORE=1`,
  coeff 60 w override.conf dispatch-shadow — potwierdzone w żywym procesie).
- Ładuje `courier_reliability.json` (feed `tools/courier_reliability.py`): per-cid `breach_rate`
  (BREACH = outcome.pickup_to_delivery_min > 35 = R6/R-35MIN), atrybucja do REALNEGO wykonawcy
  (courier_id_final), `fleet_median_breach_rate`, `confidence` wg liczności.
- Kara = `-coeff * max(0, breach_rate - fleet_median)`, gated min_gap + confidence!='low'.
- ⇒ To DOKŁADNIE rolling-history reliability penalty którego CB-01 chciał — lepiej zaprojektowany
  (gradacja proporcjonalna + confidence-gate + zero magic-number, Z2/Z3) niż szkic CB-01 (flat -25).
- Ostry przypadek BIEŻĄCEGO zlecenia: `late_pickup_committed_breach` (LIVE, per-decyzja, tier ostateczny).

## Werdykt: NIE implementować CB-01 (podwójne liczenie)
Budowa `courier_recent_delay()` + `COURIER_CIRCUIT_BREAK_PENALTY` = DRUGI penalty reliability obok A2
→ podwójne liczenie kary za niezawodność (anti-pattern SCORE-03/04) + ryzyko zbicia kuriera
<MIN_PROPOSE_SCORE. Martwe definicje (common.py:460-464) → opcjonalny retire (kosmetyka).
Pokrycie intencji CB-01: chroniczne = A2 (live), ostre = late_pickup_committed_breach (live).

## ⚠ Incydent naprawiony przy okazji: feed A2 ZAMARZŁ (LIVE impact)
Weryfikując „czy A2 realnie działa" wykryto: `dispatch-retro-learning.service` padał exit 1 (od
08-06 00:00). Root cause: logrotate (`/etc/logrotate.d/dispatch-v2`, size 100M + copytruncate, dodany
06-04) truncuje żywy `learning_log.jsonl`; `backfill_decisions_outcomes` czytał TYLKO żywy plik
(cutoff=now-14d) → po rotacji widział tylko bieżący ogon; o 04:00 (noc bez zleceń) = pusto →
„Brak danych" → cały łańcuch retro/courier_reliability/eta/a2 nie biegł → feed `courier_reliability.json`
zamarzł na 06-07. A2 (live coeff 60) jechał na coraz starszych danych; przy resecie feedu cicho
przestaje karać (fail-safe = brak kary).

**Fix** (commit `23e64ff`, tag `backfill-read-rotated-logs-2026-06-08`): backfill czyta zrotowane
`.1`/`.2.gz` w oknie --days (mtime>=cutoff + per-line ts filtr). Read-only tooling.
Weryfikacja: backfill --days14 → 4356 wierszy (0 przed); łańcuch exit 0; feed świeży (34 kurierów,
fleet_median 0.064); service Result=success; test regresji 4/4
(`tests/test_backfill_rotated_logs_2026_06_08.py`).

## Pokrewne ryzyko (NIE naprawione — niższy priorytet)
Ten sam logrotate rotuje też `shadow_decisions.jsonl`, `v319c_read_shadow_log.jsonl`, `sla_log.jsonl`.
Konsumenci tych logów (digesty SELECTION VETO / FRESH-SANITY / sla) też tracą historię tuż po rotacji
(okno tygodniowe gł. pokrywa, ale tuż po rotacji = krótkie). Niska stawka (obserwacyjne, nie live feed).
Do rozważenia: wspólny helper read-rotated dla tych konsumentów.
